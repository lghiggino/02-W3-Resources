const myForm = document.querySelector("#form");
myForm.addEventListener("submit", janFirstSunday);

const para = document.querySelector("#result");

function janFirstSunday(e){
    e.preventDefault();
    //funçãopra valer a partir daqui
}